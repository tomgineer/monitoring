{
    "scripts": {
        "dev": "npx npm-run-all --parallel dev:css dev:js",
        "dev:css": "tailwindcss -i ./tailwind/input.css -o ./app/css/tailwind.css --watch",
        "dev:js": "esbuild app/js/src/*.js --bundle --outdir=app/js --entry-names=[name]-dist --watch",
        "build": "npx npm-run-all build:css build:js",
        "build:css": "tailwindcss -i ./tailwind/input.css -o ./app/css/tailwind.css --minify",
        "build:js": "esbuild app/js/src/*.js --bundle --minify --outdir=app/js --entry-names=[name]-dist"
    },
    "devDependencies": {
        "@tailwindcss/cli": "^4.1.11",
        "daisyui": "^5.0.43",
        "esbuild": "^0.27.2",
        "npm-run-all": "^4.1.5",
        "tailwindcss": "^4.1.11"
    },
    "dependencies": {
        "chart.js": "^4.5.0"
    }
}
